import React, { Component } from 'react'

export class Business_Process extends Component {
    render() {
        return (
            <div id="Business_Process">
                <h2>Business_Process</h2>
                <p>A type of workflow in which the steps complete a service delivery process involving multiple participants (for example, to provision a service). The participants may include backend systems that automate part of the process (for example, to generate a subscriber profile) and qualified personnel who complete user tasks (for example, to ship hardware).</p>
                
            </div>
        )
    }
}

export default Business_Process
